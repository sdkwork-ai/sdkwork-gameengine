# Adaptive Web roots for sdkwork-gameengine (copy into runtime config when in-process).
# Docker entrypoint generates /etc/sdkwork/webserver/module-app-roots/sdkwork-gameengine.toml

[app_roots]
tablet_surface = "pc"

[app_roots.pc_static_by_environment]
development = "apps/sdkwork-gameengine-pc/dist/dev"
test = "apps/sdkwork-gameengine-pc/dist/test"
staging = "apps/sdkwork-gameengine-pc/dist/staging"
production = "apps/sdkwork-gameengine-pc/dist/prod"

[app_roots.static_fallback_by_environment]
development = "deployments/webserver/static"
test = "deployments/webserver/static"
staging = "deployments/webserver/static"
production = "deployments/webserver/static"
